import "./index.css";

function App() {
  // create a blob which changes its corners every 1 sec

  // create blob
  // create state to hold dynamic values for blob
  // create interval between each transition

  return (
    <div className="App">
      <div className="blob"></div>
    </div>
  );
}

export default App;
